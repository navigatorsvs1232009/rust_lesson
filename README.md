

# 🏢 Офисная логистика Dunder Mifflin (на Rust)

 *«Это не просто библиотека. Это способ выжить в офисе, где Майкл считает, что "облако" — это literally облако на небе.»*

Небольшая, но **хорошо задокументированная** библиотека на Rust, демонстрирующая **лучшие практики написания документации** в духе официальных рекомендаций Rust — но с отсылками к *The Office (US)*, потому что даже в хаосе можно быть идиоматичным.

---

## 📦 Что она делает?

Эта библиотека моделирует повседневные офисные задачи:

- ✉️ `order_paper(cloud_status: bool)` — заказывает бумагу, даже если Майкл «перенёс всё в облако»  
- ☕ `make_coffee(kind: &str)` — готовит кофе для Пэм, Джима и даже (шпионского) Дуайта

Но главное — **каждая функция полностью задокументирована**:
- С описанием на главной странице
- С аргументами, возвращаемыми значениями и разделом `# Panics`
- С рабочими, тестируемыми примерами
- Без `unwrap()` — только чистый, вежливый код

---

## 📚 Зачем это нужно?

Этот проект — **учебный пример** того, как писать документацию в Rust **по рекомендациям из официального руководства**:

- Использование `//!` и `///`
- Структура документации: кратко → подробно → пример
- Doctests, которые реально работают (`cargo test --doc`)
- Предупреждение о `missing_docs` через `#![warn(missing_docs)]`
- Markdown, ссылки, скрытые строки (`#`), обработка `?` в примерах

И всё это — без скуки. Потому что **хорошая документация не должна быть мучением**.

---

## ⚙️ Как использовать

Добавьте в ваш `Cargo.toml`:

```toml
[dependencies]
office_logistics = { path = "." }  # или замените на ваш публичный путь, если опубликуете
```

Пример использования:

```rust
use office_logistics::{order_paper, make_coffee};

fn main() {
    println!("{}", order_paper(false)); // 📄 Заказ отправлен
    println!("{}", make_coffee("latte")); // ☕ Латте готов!
}
```

---

## 🧪 Как проверить документацию

```bash
# Сгенерировать и открыть документацию в браузере
cargo doc --open

# Запустить doctests (примеры из документации)
cargo test --doc
```

Если Майкл не устроил пожарную тревогу — всё пройдёт успешно.

---

## 📝 Стиль документации

Весь код соответствует [официальным рекомендациям Rust по документированию](https://doc.rust-lang.org/rustdoc/how-to-write-documentation.html):

- Главная страница с реалистичным примером
- Все `pub` элементы задокументированы
- Использованы разделы `# Arguments`, `# Returns`, `# Panics`
- Примеры можно копировать и запускать
- Нет `unwrap()` — только `Result` или чёткое поведение без ошибок

---

## 🎭 Вдохновлено

- **Майклом Скоттом** — за веру в "облако"
- **Пэм** — за терпение и любовь к порядку
- **Джимом** — за иронию и умение объяснять сложно — просто
- **Дуайтом** — за желание документировать всё… даже уровень угрозы пончиков

---

## 📚 Дальнейшее чтение

- [Официальное руководство по документированию в Rust](https://doc.rust-lang.org/rustdoc/how-to-write-documentation.html)
- [`std::env::args()`](https://doc.rust-lang.org/std/env/fn.args.html) — образец идеальной документации
- [The Office (US), сезон 2, эпизод "The Fire"](https://www.imdb.com/title/tt0669933/) — источник вдохновения для `order_paper(true)`

---

> *«Я не просто пью кофе. Я участвую в распределённой системе доставки, построенной на доверии, каналах и надежде, что Майкл не включит пожарную сигнализацию.»*  
> — Пэм Бисли

---

Спасибо, что заглянули!  
☕📄 Теперь и вы можете документировать как команда Dunder Mifflin — с душой, юмором и без паники.
